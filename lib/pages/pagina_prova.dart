import 'package:flutter/material.dart';

class ProvaPage extends StatelessWidget {
  const ProvaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("pagina di prova"));
  }
}
